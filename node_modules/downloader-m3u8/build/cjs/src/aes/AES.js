"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AES = void 0;
var cryptos = require('crypto');
var fs = require('fs');
var AES = /** @class */ (function () {
    function AES(key, iv) {
        this.key = Buffer.from(key, 'hex');
        this.iv = Buffer.from(iv, 'hex');
    }
    AES.prototype.aesEncryptNew = function (buff) {
        var cipher = cryptos.createCipheriv('aes-128-cbc', this.key, this.iv);
        return cipher.update(buff, '', 'hex') + cipher.final('hex');
    };
    AES.prototype.aesDecryptNew = function (buff) {
        var decipher = cryptos.createDecipheriv('aes-128-cbc', this.key, this.iv);
        return decipher.update(buff, '', 'hex') + decipher.final('hex');
    };
    return AES;
}());
exports.AES = AES;
